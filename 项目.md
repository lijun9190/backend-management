# 后端认证鉴权方案评估

## 评估范围

本评估聚焦当前项目已经实现的认证与鉴权链路，包括：

- 登录接口自行校验账号密码并签发 JWT
- 网关对 token 做基础校验
- 业务服务通过 `JwtAuthenticationFilter` 恢复登录态
- 接口权限通过 `@PreAuthorize("@perm.hasPermission(...)")` 控制
- 菜单、页面、按钮权限基于 `sys_menu` / `sys_role_menu` / `sys_user_role` 建模

结论先行：

- 作为内部后台管理系统、RBAC 明确的中小型项目骨架，这套方案具备真实项目落地基础
- 作为直接面向生产的完整安全方案，目前仍有一批明显偏 demo 的部分，建议在上线前补强

## 已具备生产可用基础

### 1. 认证链路清晰，职责拆分合理

当前方案已经形成比较完整的请求链路：

- 登录由 `AuthController` 和 `AuthServiceImpl` 处理
- 登录成功后生成 JWT，并把登录态缓存到 Redis
- 网关先校验 token 是否存在、是否合法、是否仍在 Redis 中
- 请求进入服务后，`JwtAuthenticationFilter` 再把登录用户恢复到 `SecurityContextHolder` 和 `LoginUserContext`

这套实现对于后台管理系统来说是可落地的，问题定位也相对直接。

### 2. 权限模型与后台系统高度匹配

当前项目不是做通用 OAuth2 平台，而是做典型管理后台，因此采用：

- 角色
- 菜单
- 按钮权限码
- 方法级 `@PreAuthorize`

这种模型是合理的，也很适合系统管理、用户管理、日志管理这类场景。前后端的权限码也已经基本打通。

### 3. 方法级鉴权简单直接

项目没有引入复杂的动态 URL 决策器，而是直接在 Controller 上声明权限要求。这种方式在真实项目里非常常见，优点是：

- 可读性强
- 接口需要什么权限一眼能看见
- 不必维护额外的 URL 到权限码映射引擎

对于接口数量可控、团队规模不大的项目，这是可维护的选择。

### 4. 已考虑“登录态失效”和“登出后失效”

当前 JWT 不是完全无状态的“签发后不可控”模式，而是结合 Redis 保存登录态。这样可以支持：

- 登出后 token 失效
- 服务端判断 token 是否已失效
- 后续扩展强制下线、单点登录等能力

这比纯本地 JWT 校验更贴近真实项目需求。

### 5. 已具备密码加密和基础审计能力

当前方案里已经使用 BCrypt 处理密码，并且有登录日志、操作日志等基础审计能力。这些都属于真实项目中很重要的基础能力，不是纯演示性质。

### 6. 前后端权限数据链路已打通

当前权限并不只停留在后端接口校验，还已经联动到前端：

- 后端返回 `roles / permissions / menus`
- 前端根据 `menus` 生成动态路由
- 前端根据 `permissions` 控制按钮显隐

这说明项目已经具备一套完整的 RBAC 闭环，而不只是孤立的接口保护。

## 目前仍偏 demo 的部分

### 1. 密钥和基础配置直接写在配置文件中

当前 `security.jwt.secret`、Redis 地址和密码都直接写在 `application.yml` 中。这在演示项目里常见，但不适合生产环境。生产环境至少需要：

- 使用环境变量或配置中心注入
- 区分不同环境的密钥
- 避免敏感信息进入代码仓库

这是当前最明显的 demo 痕迹之一。

### 2. 登录安全能力还不够

目前登录流程主要完成了：

- 用户存在校验
- 状态校验
- 密码校验
- 角色存在校验

但缺少真实生产环境里常见的防护，例如：

- 验证码
- 登录限流
- 连续失败锁定
- IP 风险识别
- 异地登录提醒
- 多因素认证

如果面向公网或更高风险环境，这一块明显不够。

### 3. 会话治理能力不完整

当前已经把 token 和用户信息放进 Redis，但会话治理仍然比较基础，缺少：

- refresh token
- access token 刷新机制
- 续签策略
- 多端登录策略
- 设备维度会话管理
- 强制踢人下线的完整流程

从代码上看，`loginUserKey(userId)` 已经预留了扩展空间，但现在还没有真正形成完整会话治理方案。

### 4. 鉴权依赖人工声明，存在漏标风险

当前接口权限控制主要靠开发者手动写：

- `@PreAuthorize("@perm.hasPermission('xxx')")`

这种方式简单直接，但真实项目中要注意一个问题：一旦某个接口漏写注解，就可能直接暴露。也就是说，这套方案“可用”，但对代码评审、测试覆盖和安全规范的依赖比较高。

### 5. 更适合后台 RBAC，不适合复杂统一认证平台

当前方案很适合：

- 单后台
- 内部管理系统
- 明确角色权限模型

但如果未来要支持：

- 多系统单点登录
- 第三方登录
- 统一身份中心
- 多租户复杂隔离
- 外部开放授权

那这套实现会逐渐吃力，需要演进到更标准的认证体系，例如 Spring Authorization Server、Keycloak 或公司统一 IAM 平台。

### 6. 缺少更系统的安全基线建设

从当前代码能看到基础鉴权能力，但还看不到一套完整的上线安全基线，例如：

- HTTPS 强制策略说明
- 网关信任头传递边界
- 安全响应头策略
- CORS 明确收敛
- 关键安全事件告警
- 敏感操作二次确认
- 更完整的认证与越权自动化测试

这些不是说系统不能运行，而是说明它还没有完全从“可运行 demo”走到“成熟生产系统”。

## 生产落地判断

### 可以直接作为真实项目基础骨架的场景

以下场景可以继续沿用当前实现，并在此基础上逐步补强：

- 公司内部后台
- 中小型管理平台
- 用户规模可控的 RBAC 系统
- 以菜单、页面、按钮权限为主的管理系统

### 不建议以当前形态直接上线的场景

以下场景不建议用当前版本直接上线：

- 面向公网且存在明显攻击面的系统
- 对合规审计要求高的系统
- 需要复杂登录风控的系统
- 多系统统一认证平台
- 第三方授权和开放平台场景

## 改造优先级建议

### P0：上线前必须补

- 将 JWT 密钥、Redis 密码等敏感配置移出仓库
- 补充登录限流、失败锁定、验证码等基础防护
- 明确 token 失效、登出、续期策略
- 补充认证与越权测试用例

### P1：生产稳定性建议补

- 增加设备维度会话管理
- 支持强制下线和挤占登录
- 增加安全审计和异常登录告警
- 收紧网关与服务间的身份传递边界

### P2：中长期演进

- 如后续系统数量增多，评估统一认证中心
- 如后续接入第三方登录，评估 OAuth2 / OIDC 标准体系
- 如后续租户模型复杂化，重构权限与身份边界

## 总结

这套方案不是“只能演示不能落地”的玩具实现。

它已经具备真实后台项目常见的认证、登录态恢复、权限校验、前后端权限联动等核心能力，完全可以作为真实项目的第一版骨架。

但它目前更像“具备生产基础的工程起点”，而不是“已经打磨完成的生产安全方案”。如果项目准备正式上线，建议优先补齐登录安全、敏感配置治理、会话治理和安全测试这几块。



测试是否提交信息是否中文
